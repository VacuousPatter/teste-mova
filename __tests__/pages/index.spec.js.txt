import { mount } from '@vue/test-utils'
import Index from '@/pages/index.vue'

test('não aceita pesquisar capital sem informar a capital', () => {
  const route = {
    query: {}
  }

  const wrapper = mount(Index, {
    mocks: {
      $route: route
    }
  })

  // const select = wrapper.find('#select-filtrar-por')

  expect(wrapper).toBeTruthy()
})
